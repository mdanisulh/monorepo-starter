pre-commit:
  commands:
    check:
      run: bun sort-package-json && bun biome check --write --error-on-warnings --no-errors-on-unmatched --colors=off {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: bun commitlint --edit $1
